namespace = cleanup_1644

cleanup_1644.1 = {
	type = country_event
	fire_only_once = no
	hidden = yes
	dynamic_historical_event = {
		tag = QNG
		tag = CSH
		tag = WSG
		tag = CXI
		tag = MNG
		from = 1644.1.1
		to = 1836.12.31
		monthly_chance = 100
	}

	trigger = {
		OR = {
			tag = QNG
			tag = CSH
			tag = WSG
			tag = CXI
			tag = MNG
		}
	}

	immediate = {
		every_character = {
			limit = {
				is_mercenary_leader = yes
			}
			kill_character_silently = this
	}
	}

	option = { name = OK }
}

